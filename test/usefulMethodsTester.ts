/**
 * Created by jacke on 2/3/2017.
 */
/**
 * Created by Austin on 2017-01-24.
 */

/*
import {expect} from 'chai';
import Log from "../src/Util";
import {InsightResponse} from "../src/controller/IInsightFacade";
import InsightFacade from "../src/controller/InsightFacade";
var fs = require("fs");

describe("usefulMethodsTester", function() {
    let facade: any = null;
    let zipContent: any;
    before(function() {
        //zipContent = fs.readFileSync("courses.zip").toString("base64");
    });
    beforeEach(function() {
        facade = new InsightFacade();
    });
    afterEach(function() {
        facade = null;
    });

    it("testing filterManager null object", function () {
        try {
            facade.filterManager({},{});
            expect.fail();
        }catch(err){console.log(err);}
        return ;
    });

    it("testing hasValidOptions", function() {
        console.log(facade.hasValidOptions(""));
        return;
    });

    it("testing keyToJsonKey", function() {
        console.log(facade.keyToJsonKey("courses_dept"));
        return;
    });
});
*/